{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"  // Match the Node.js version in your client/package.json
  }
}
